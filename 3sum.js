const threeSum =  function(nums) {
  const answer = [];
  // to compare each number by order if sum is too high, add left one vice versa right one. 
  nums.sort((a, b) => a-b);
 
  for (let main = )

}