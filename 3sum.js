const threeSum =  function(nums) {
  const answer = [];
  // to compare each number by order if sum is too high, add left one vice versa right one. it save the time easy to find dupulication.
  nums.sort((a, b) => a-b);
 
 

}